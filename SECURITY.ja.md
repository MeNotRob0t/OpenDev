# セキュリティ

## 重要

AI 生成のセキュリティレポートは受け付けていません。大量に寄せられますが、すべてを確認するリソースがありません。送信された場合、プロジェクトから自動的に禁止されます。

## 脅威モデル

### 概要

OpenCode はローカルマシンで実行される AI 搭載のコーディングアシスタントです。シェル実行、ファイル操作、Web アクセスなどの強力なツールにアクセスできるエージェントシステムを提供します。

### サンドボックスなし

OpenCode はエージェントを **サンドボックス化しません**。権限システムは UX 機能として存在し、ユーザーがエージェントの実行アクションを把握できるようにするものです。コマンド実行やファイル書き込みの前に確認を求めます。しかし、セキュリティ分離を提供するようには設計されていません。

真の分離が必要な場合は、OpenCode を Docker コンテナや VM 内で実行してください。

### サーバーモード

サーバーモードはオプトインのみです。有効にする場合、`OPENCODE_SERVER_PASSWORD` を設定して HTTP Basic Auth を要求します。設定しない場合、サーバーは未認証で実行されます（警告付き）。サーバーの保護はエンドユーザーの責任です。提供される機能は脆弱性ではありません。

### 対象外

| カテゴリ | 根拠 |
| ------------------------------- | ----------------------------------------------------------------------- |
| **オプトイン時のサーバーアクセス** | サーバーモードを有効にした場合、API アクセスは想定される動作 |
| **サンドボックス脱出** | 権限システムはサンドボックスではない（上記参照） |
| **LLM プロバイダーのデータ処理** | 設定した LLM プロバイダーに送信されるデータは、そのポリシーに従う |
| **MCP サーバーの動作** | 設定した外部 MCP サーバーは信頼境界の外 |
| **悪意のある設定ファイル** | ユーザーは自身の設定を制御しており、変更は攻撃ベクトルではない |

---

# セキュリティ問題の報告

責任ある開示に感謝し、貢献を最大限評価します。

セキュリティ問題を報告するには、GitHub Security Advisory の ["Report a Vulnerability"](https://github.com/anomalyco/opencode/security/advisories/new) タブを使用してください。

チームから次の手順を示す返信を送信します。初回返信後、セキュリティチームは修正と完全な公開に向けた進捗を通知し、追加情報やガイダンスを求める場合があります。

## エスカレーション

6 営業日以内に報告の確認を受け取れない場合は、security@anoma.ly までメールでご連絡ください。