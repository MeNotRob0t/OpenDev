# 보안

## 중요

AI 생성 보안 보고서는 받지 않습니다. 많은 수를 접수하지만 모두 검토할 리소스가 없습니다. 제출 시 프로젝트에서 자동으로 차단됩니다.

## 위협 모델

### 개요

OpenCode는 사용자 머신에서 로컬로 실행되는 AI 기반 코딩 어시스턴트입니다. 쉘 실행, 파일 작업, 웹 액세스 등 강력한 도구에 접근하는 에이전트 시스템을 제공합니다.

### 샌드박스 없음

OpenCode는 에이전트를 **샌드박스화하지 않습니다**. 권한 시스템은 사용자가 에이전트 행동을 인지하도록 돕는 UX 기능으로, 명령 실행, 파일 쓰기 전에 확인을 요청합니다. 하지만 보안 격리를 제공하도록 설계되지 않았습니다.

진정한 격리가 필요하면 OpenCode를 Docker 컨테이너나 VM 내부에서 실행하세요.

### 서버 모드

서버 모드는 옵트인만 가능합니다. 활성화 시 `OPENCODE_SERVER_PASSWORD`를 설정해 HTTP Basic Auth를 요구하세요. 없으면 서버는 인증 없이 실행됩니다(경고 포함). 서버 보안은 최종 사용자 책임이며, 제공되는 기능은 취약점이 아닙니다.

### 범위 외

| 카테고리 | 근거 |
| ------------------------------- | ----------------------------------------------------------------------- |
| **옵트인 시 서버 접근** | 서버 모드 활성화 시 API 접근은 예상 동작 |
| **샌드박스 탈출** | 권한 시스템은 샌드박스가 아님 (위 참조) |
| **LLM 제공자 데이터 처리** | 구성된 LLM 제공자에게 전송된 데이터는 해당 정책 적용 |
| **MCP 서버 동작** | 구성한 외부 MCP 서버는 신뢰 경계 밖 |
| **악성 설정 파일** | 사용자가 자신의 설정을 제어하며, 수정은 공격 벡터가 아님 |

---

# 보안 문제 신고

책임 있는 공개를 위한 노력에 감사하며, 기여를 최대한 인정하겠습니다.

보안 문제를 신고하려면 GitHub Security Advisory ["Report a Vulnerability"](https://github.com/anomalyco/opencode/security/advisories/new) 탭을 사용하세요.

팀에서 다음 단계에 대한 회신을 보냅니다. 초기 회신 후 보안팀은 수정과 전체 공개 진행 상황을 알리고, 추가 정보나 지침을 요청할 수 있습니다.

## 에스컬레이션

6 영업일 내에 신고 확인을 받지 못하면 security@anoma.ly로 이메일을 보내세요.