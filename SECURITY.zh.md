# 安全

## 重要

我们不接受 AI 生成的安全报告。我们收到大量此类报告，完全没有资源逐一审查。提交者将被自动禁止参与项目。

## 威胁模型

### 概述

OpenCode 是在本地机器上运行的 AI 编码助手。它提供了一个拥有强大工具访问权限的代理系统，包括 Shell 执行、文件操作和 Web 访问。

### 无沙箱

OpenCode **不对** 代理进行沙箱隔离。权限系统作为 UX 功能存在，帮助用户了解代理正在执行的操作 —— 在执行命令、写入文件等前会要求确认。但它并非设计用于提供安全隔离。

如果需要真正的隔离，请在 Docker 容器或虚拟机中运行 OpenCode。

### 服务器模式

服务器模式仅为选择加入。启用时，设置 `OPENCODE_SERVER_PASSWORD` 以要求 HTTP Basic Auth。否则服务器将以未认证方式运行（带警告）。服务器安全由最终用户负责 —— 其提供的任何功能均不构成漏洞。

### 范围外

| 类别 | 理由 |
| ------------------------------- | ----------------------------------------------------------------------- |
| **选择加入时的服务器访问** | 启用服务器模式即表示预期会有 API 访问 |
| **沙箱逃逸** | 权限系统不是沙箱（见上文） |
| **LLM 提供商数据处理** | 发送给配置的 LLM 提供商的数据受其政策管辖 |
| **MCP 服务器行为** | 您配置的外部 MCP 服务器位于信任边界之外 |
| **恶意配置文件** | 用户控制自己的配置；修改它不是攻击向量 |

---

# 报告安全问题

我们感谢您负责任地披露发现，并将尽最大努力确认您的贡献。

请使用 GitHub Security Advisory ["Report a Vulnerability"](https://github.com/anomalyco/opencode/security/advisories/new) 标签页报告安全问题。

团队将发送回复，说明处理报告的后续步骤。初步回复后，安全团队将随时向您通报修复进展和完整公告，并可能请求更多信息或指导。

## 升级

如果在 6 个工作日内未收到报告确认，可发送邮件至 security@anoma.ly